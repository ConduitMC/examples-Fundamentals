
buildscript {
    repositories {
        mavenLocal()
        jcenter()
        maven {
            url "https://repo.conduit.systems/repositories/releases/"
        }
    }

    dependencies {
        classpath "systems.conduit:Stream:1.0.1"
    }
}

apply plugin: "Stream"

conduit {
    version = "0.0.5"
    minecraft = "20w48a"
    java = "1.8"
}
